

local FzHubV2 = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local UICorner = Instance.new("UICorner")
local Title = Instance.new("TextLabel")
local FarmKillsButton = Instance.new("TextButton")
local UICorner_2 = Instance.new("UICorner")
local InfoButton = Instance.new("TextButton")
local UICorner_3 = Instance.new("UICorner")
local MinimizeButton = Instance.new("TextButton")
local UICorner_4 = Instance.new("UICorner")
local TitleImage = Instance.new("ImageLabel")
local UICorner_5 = Instance.new("UICorner")
local AutoResetButton = Instance.new("TextButton")
local UICorner_6 = Instance.new("UICorner")
local SetSpawnButton = Instance.new("TextButton")
local UICorner_7 = Instance.new("UICorner")
local OptimizationButton = Instance.new("TextButton")
local UICorner_8 = Instance.new("UICorner")
local RejoinButton = Instance.new("TextButton")
local UICorner_9 = Instance.new("UICorner")
local KillsCountButton = Instance.new("TextButton")
local UICorner_10 = Instance.new("UICorner")
local InformationFrame = Instance.new("Frame")
local UICorner_11 = Instance.new("UICorner")
local ImageLabel = Instance.new("ImageLabel")
local UICorner_12 = Instance.new("UICorner")
local TextLabel = Instance.new("TextLabel")
local TextLabel_2 = Instance.new("TextLabel")
local TextLabel_3 = Instance.new("TextLabel")
local KillsGainedFrame = Instance.new("Frame")
local UICorner_13 = Instance.new("UICorner")
local KillsGained = Instance.new("TextLabel")
local Time = Instance.new("TextLabel")
local _0000 = Instance.new("TextLabel")
local ImageButton = Instance.new("ImageButton")
local WelcomeFrame = Instance.new("Frame")
local TextLabel_4 = Instance.new("TextLabel")
local UICorner_14 = Instance.new("UICorner")
local ImageLabel_2 = Instance.new("ImageLabel")

--Properties:

FzHubV2.Name = "FzHubV2"
FzHubV2.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
FzHubV2.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
FzHubV2.ResetOnSpawn = false

MainFrame.Name = "MainFrame"
MainFrame.Parent = FzHubV2
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
MainFrame.BorderColor3 = Color3.fromRGB(0, 0, 0)
MainFrame.BorderSizePixel = 0
MainFrame.Position = UDim2.new(0.381298691, 0, 0.0755007714, 0)
MainFrame.Size = UDim2.new(0, 416, 0, 506)
MainFrame.Visible = false

UICorner.Parent = MainFrame

Title.Name = "Title"
Title.Parent = MainFrame
Title.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Title.BackgroundTransparency = 1.000
Title.BorderColor3 = Color3.fromRGB(0, 0, 0)
Title.BorderSizePixel = 0
Title.Position = UDim2.new(0.177347764, 0, 0.0158102773, 0)
Title.Size = UDim2.new(0, 153, 0, 22)
Title.Font = Enum.Font.SourceSansBold
Title.Text = "Fz Hub v2"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 21.000
Title.TextWrapped = true

FarmKillsButton.Name = "FarmKillsButton"
FarmKillsButton.Parent = MainFrame
FarmKillsButton.BackgroundColor3 = Color3.fromRGB(60, 60, 100)
FarmKillsButton.BorderColor3 = Color3.fromRGB(0, 0, 0)
FarmKillsButton.BorderSizePixel = 0
FarmKillsButton.Position = UDim2.new(0, 0, 0.0733654052, 0)
FarmKillsButton.Size = UDim2.new(0, 200, 0, 41)
FarmKillsButton.Font = Enum.Font.SourceSansBold
FarmKillsButton.Text = "Farm Kills"
FarmKillsButton.TextColor3 = Color3.fromRGB(255, 255, 255)
FarmKillsButton.TextSize = 19.000

UICorner_2.Parent = FarmKillsButton

InfoButton.Name = "InfoButton"
InfoButton.Parent = MainFrame
InfoButton.BackgroundColor3 = Color3.fromRGB(60, 60, 100)
InfoButton.BorderColor3 = Color3.fromRGB(0, 0, 0)
InfoButton.BorderSizePixel = 0
InfoButton.Position = UDim2.new(0.504807711, 0, 0.0733654052, 0)
InfoButton.Size = UDim2.new(0, 206, 0, 41)
InfoButton.Font = Enum.Font.SourceSansBold
InfoButton.Text = "Information"
InfoButton.TextColor3 = Color3.fromRGB(255, 255, 255)
InfoButton.TextSize = 19.000

UICorner_3.Parent = InfoButton

MinimizeButton.Name = "MinimizeButton"
MinimizeButton.Parent = MainFrame
MinimizeButton.BackgroundColor3 = Color3.fromRGB(101, 106, 166)
MinimizeButton.BorderColor3 = Color3.fromRGB(0, 0, 0)
MinimizeButton.BorderSizePixel = 0
MinimizeButton.Position = UDim2.new(0.90625, 0, 0, 0)
MinimizeButton.Size = UDim2.new(0, 39, 0, 38)
MinimizeButton.Font = Enum.Font.SourceSans
MinimizeButton.Text = "-"
MinimizeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
MinimizeButton.TextSize = 37.000
MinimizeButton.TextWrapped = true

UICorner_4.Parent = MinimizeButton

TitleImage.Name = "TitleImage"
TitleImage.Parent = MainFrame
TitleImage.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
TitleImage.BorderColor3 = Color3.fromRGB(0, 0, 0)
TitleImage.BorderSizePixel = 0
TitleImage.Position = UDim2.new(0.346153855, 0, 0.181818187, 0)
TitleImage.Size = UDim2.new(0, 131, 0, 123)
TitleImage.Image = "rbxassetid://132782852102479"

UICorner_5.Parent = TitleImage

AutoResetButton.Name = "AutoResetButton"
AutoResetButton.Parent = MainFrame
AutoResetButton.BackgroundColor3 = Color3.fromRGB(120, 70, 180)
AutoResetButton.BorderColor3 = Color3.fromRGB(0, 0, 0)
AutoResetButton.BorderSizePixel = 0
AutoResetButton.Position = UDim2.new(0.115384616, 0, 0.466403157, 0)
AutoResetButton.Size = UDim2.new(0, 322, 0, 33)
AutoResetButton.Font = Enum.Font.SourceSansBold
AutoResetButton.Text = "AutoReset: OFF"
AutoResetButton.TextColor3 = Color3.fromRGB(255, 255, 255)
AutoResetButton.TextSize = 18.000

UICorner_6.Parent = AutoResetButton

SetSpawnButton.Name = "SetSpawnButton"
SetSpawnButton.Parent = MainFrame
SetSpawnButton.BackgroundColor3 = Color3.fromRGB(120, 70, 180)
SetSpawnButton.BorderColor3 = Color3.fromRGB(0, 0, 0)
SetSpawnButton.BorderSizePixel = 0
SetSpawnButton.Position = UDim2.new(0.112980768, 0, 0.543478251, 0)
SetSpawnButton.Size = UDim2.new(0, 322, 0, 33)
SetSpawnButton.Font = Enum.Font.SourceSansBold
SetSpawnButton.Text = "Set Spawn"
SetSpawnButton.TextColor3 = Color3.fromRGB(255, 255, 255)
SetSpawnButton.TextSize = 18.000

UICorner_7.Parent = SetSpawnButton

OptimizationButton.Name = "OptimizationButton"
OptimizationButton.Parent = MainFrame
OptimizationButton.BackgroundColor3 = Color3.fromRGB(120, 70, 180)
OptimizationButton.BorderColor3 = Color3.fromRGB(0, 0, 0)
OptimizationButton.BorderSizePixel = 0
OptimizationButton.Position = UDim2.new(0.112980768, 0, 0.620553374, 0)
OptimizationButton.Size = UDim2.new(0, 322, 0, 33)
OptimizationButton.Font = Enum.Font.SourceSansBold
OptimizationButton.Text = "Optimization"
OptimizationButton.TextColor3 = Color3.fromRGB(255, 255, 255)
OptimizationButton.TextSize = 18.000

UICorner_8.Parent = OptimizationButton

RejoinButton.Name = "RejoinButton"
RejoinButton.Parent = MainFrame
RejoinButton.BackgroundColor3 = Color3.fromRGB(120, 70, 180)
RejoinButton.BorderColor3 = Color3.fromRGB(0, 0, 0)
RejoinButton.BorderSizePixel = 0
RejoinButton.Position = UDim2.new(0.112980768, 0, 0.697628438, 0)
RejoinButton.Size = UDim2.new(0, 322, 0, 33)
RejoinButton.Font = Enum.Font.SourceSansBold
RejoinButton.Text = "Rejoin"
RejoinButton.TextColor3 = Color3.fromRGB(255, 255, 255)
RejoinButton.TextSize = 18.000

UICorner_9.Parent = RejoinButton

KillsCountButton.Name = "KillsCountButton"
KillsCountButton.Parent = MainFrame
KillsCountButton.BackgroundColor3 = Color3.fromRGB(120, 70, 180)
KillsCountButton.BorderColor3 = Color3.fromRGB(0, 0, 0)
KillsCountButton.BorderSizePixel = 0
KillsCountButton.Position = UDim2.new(0.112980768, 0, 0.776679814, 0)
KillsCountButton.Size = UDim2.new(0, 322, 0, 33)
KillsCountButton.Font = Enum.Font.SourceSansBold
KillsCountButton.Text = "Kills Count"
KillsCountButton.TextColor3 = Color3.fromRGB(255, 255, 255)
KillsCountButton.TextSize = 18.000

UICorner_10.Parent = KillsCountButton

InformationFrame.Name = "InformationFrame"
InformationFrame.Parent = MainFrame
InformationFrame.BackgroundColor3 = Color3.fromRGB(32, 32, 54)
InformationFrame.BorderColor3 = Color3.fromRGB(0, 0, 0)
InformationFrame.BorderSizePixel = 0
InformationFrame.Position = UDim2.new(0, 0, 0.169960439, 0)
InformationFrame.Size = UDim2.new(0, 416, 0, 420)
InformationFrame.Visible = false

UICorner_11.Parent = InformationFrame

ImageLabel.Parent = InformationFrame
ImageLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
ImageLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
ImageLabel.BorderSizePixel = 0
ImageLabel.Position = UDim2.new(0.024038462, 0, 0.0294372551, 0)
ImageLabel.Size = UDim2.new(0, 89, 0, 85)
ImageLabel.Image = "rbxassetid://132782852102479"

UICorner_12.Parent = ImageLabel

TextLabel.Parent = InformationFrame
TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
TextLabel.BackgroundTransparency = 1.000
TextLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
TextLabel.BorderSizePixel = 0
TextLabel.Position = UDim2.new(0.160520852, 0, 0.0515245721, 0)
TextLabel.Size = UDim2.new(0, 153, 0, 20)
TextLabel.Font = Enum.Font.SourceSansBold
TextLabel.Text = "Fz Hub v2"
TextLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TextLabel.TextSize = 21.000
TextLabel.TextWrapped = true

TextLabel_2.Parent = InformationFrame
TextLabel_2.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
TextLabel_2.BackgroundTransparency = 1.000
TextLabel_2.BorderColor3 = Color3.fromRGB(0, 0, 0)
TextLabel_2.BorderSizePixel = 0
TextLabel_2.Position = UDim2.new(0.160520852, 0, 0.106286474, 0)
TextLabel_2.Size = UDim2.new(0, 153, 0, 20)
TextLabel_2.Font = Enum.Font.SourceSansBold
TextLabel_2.Text = "Status: 🟢"
TextLabel_2.TextColor3 = Color3.fromRGB(255, 255, 255)
TextLabel_2.TextSize = 21.000
TextLabel_2.TextWrapped = true

TextLabel_3.Parent = InformationFrame
TextLabel_3.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
TextLabel_3.BackgroundTransparency = 1.000
TextLabel_3.BorderColor3 = Color3.fromRGB(0, 0, 0)
TextLabel_3.BorderSizePixel = 0
TextLabel_3.Position = UDim2.new(0.254270852, 0, 0.542000771, 0)
TextLabel_3.Size = UDim2.new(0, 207, 0, 20)
TextLabel_3.Font = Enum.Font.SourceSansBold
TextLabel_3.Text = "Nothing to put here lol"
TextLabel_3.TextColor3 = Color3.fromRGB(255, 255, 255)
TextLabel_3.TextSize = 21.000
TextLabel_3.TextWrapped = true

KillsGainedFrame.Name = "KillsGainedFrame"
KillsGainedFrame.Parent = MainFrame
KillsGainedFrame.BackgroundColor3 = Color3.fromRGB(26, 29, 44)
KillsGainedFrame.BorderColor3 = Color3.fromRGB(0, 0, 0)
KillsGainedFrame.BorderSizePixel = 0
KillsGainedFrame.Position = UDim2.new(-0.78125, 0, 0.154393077, 0)
KillsGainedFrame.Size = UDim2.new(0, 208, 0, 75)
KillsGainedFrame.Visible = false

UICorner_13.Parent = KillsGainedFrame

KillsGained.Name = "KillsGained"
KillsGained.Parent = KillsGainedFrame
KillsGained.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
KillsGained.BackgroundTransparency = 1.000
KillsGained.BorderColor3 = Color3.fromRGB(0, 0, 0)
KillsGained.BorderSizePixel = 0
KillsGained.Position = UDim2.new(0, 0, 0.127253696, 0)
KillsGained.Size = UDim2.new(0, 208, 0, 30)
KillsGained.Font = Enum.Font.SourceSansBold
KillsGained.Text = "Kills: 0 (Gained:  0)"
KillsGained.TextColor3 = Color3.fromRGB(255, 255, 255)
KillsGained.TextSize = 21.000

Time.Name = "Time"
Time.Parent = KillsGainedFrame
Time.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Time.BackgroundTransparency = 1.000
Time.BorderColor3 = Color3.fromRGB(0, 0, 0)
Time.BorderSizePixel = 0
Time.Position = UDim2.new(-0.0625, 0, 0.514339626, 0)
Time.Size = UDim2.new(0, 200, 0, 50)
Time.Font = Enum.Font.SourceSans
Time.Text = "Time: "
Time.TextColor3 = Color3.fromRGB(145, 145, 145)
Time.TextSize = 18.000

_0000.Name = "00:00"
_0000.Parent = Time
_0000.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
_0000.BackgroundTransparency = 1.000
_0000.BorderColor3 = Color3.fromRGB(0, 0, 0)
_0000.BorderSizePixel = 0
_0000.Position = UDim2.new(0.182500005, 0, 0.0193713382, 0)
_0000.Size = UDim2.new(0, 195, 0, 49)
_0000.Font = Enum.Font.SourceSans
_0000.Text = "00:00"
_0000.TextColor3 = Color3.fromRGB(145, 145, 145)
_0000.TextSize = 18.000

ImageButton.Parent = FzHubV2
ImageButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
ImageButton.BorderColor3 = Color3.fromRGB(0, 0, 0)
ImageButton.BorderSizePixel = 0
ImageButton.Position = UDim2.new(0.00941167492, 0, 0.489984602, 0)
ImageButton.Size = UDim2.new(0, 85, 0, 78)
ImageButton.Visible = false
ImageButton.Image = "rbxassetid://132782852102479"

WelcomeFrame.Name = "WelcomeFrame"
WelcomeFrame.Parent = FzHubV2
WelcomeFrame.BackgroundColor3 = Color3.fromRGB(3, 3, 3)
WelcomeFrame.BackgroundTransparency = 0.200
WelcomeFrame.BorderColor3 = Color3.fromRGB(0, 0, 0)
WelcomeFrame.BorderSizePixel = 0
WelcomeFrame.Position = UDim2.new(0.252134442, 0, 0.0970724225, 0)
WelcomeFrame.Size = UDim2.new(0, 534, 0, 532)

TextLabel_4.Parent = WelcomeFrame
TextLabel_4.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
TextLabel_4.BackgroundTransparency = 1.000
TextLabel_4.BorderColor3 = Color3.fromRGB(0, 0, 0)
TextLabel_4.BorderSizePixel = 0
TextLabel_4.Position = UDim2.new(0.171109438, 0, 0.366513252, 0)
TextLabel_4.Size = UDim2.new(0, 200, 0, 44)
TextLabel_4.Font = Enum.Font.SourceSansBold
TextLabel_4.Text = "Welcome, "
TextLabel_4.TextColor3 = Color3.fromRGB(255, 255, 255)
TextLabel_4.TextSize = 31.000

UICorner_14.Parent = WelcomeFrame

ImageLabel_2.Parent = WelcomeFrame
ImageLabel_2.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
ImageLabel_2.BorderColor3 = Color3.fromRGB(0, 0, 0)
ImageLabel_2.BorderSizePixel = 0
ImageLabel_2.Position = UDim2.new(0.384994686, 0, 0.108695641, 0)
ImageLabel_2.Size = UDim2.new(0, 122, 0, 121)
ImageLabel_2.Image = "rbxassetid://132782852102479"

-- Scripts:

local function BJSONBO_fake_script() -- InfoButton.LocalScript 
	local script = Instance.new('LocalScript', InfoButton)

	
	local button = script.Parent
	local frame = script.Parent.Parent:WaitForChild("InformationFrame") -- change "Frame" if named differently
	
	-- optional: start hidden
	frame.Visible = false
	frame.BackgroundTransparency = 1
	
	-- tween service for smooth fade
	local TweenService = game:GetService("TweenService")
	
	-- settings
	local fadeTime = 0.3 -- seconds for fade animation
	
	-- function to fade in
	local function fadeIn()
		frame.Visible = true
		local tween = TweenService:Create(frame, TweenInfo.new(fadeTime), {BackgroundTransparency = 0})
		tween:Play()
	end
	
	-- function to fade out
	local function fadeOut()
		local tween = TweenService:Create(frame, TweenInfo.new(fadeTime), {BackgroundTransparency = 1})
		tween:Play()
		tween.Completed:Connect(function()
			frame.Visible = false
		end)
	end
	
	-- toggle on button click
	local isVisible = false
	button.MouseButton1Click:Connect(function()
		isVisible = not isVisible
		if isVisible then
			fadeIn()
		else
			fadeOut()
		end
	end)
	
end
coroutine.wrap(BJSONBO_fake_script)()
local function IZGC_fake_script() -- MinimizeButton.LocalScript 
	local script = Instance.new('LocalScript', MinimizeButton)

	
	local button = script.Parent
	local mainFrame = button.Parent -- MainFrame
	local screenGui = mainFrame.Parent -- ScreenGui
	local imageButton = screenGui:WaitForChild("ImageButton")
	
	button.MouseButton1Click:Connect(function()
		mainFrame.Visible = false
		imageButton.Visible = true
	end)
	
end
coroutine.wrap(IZGC_fake_script)()
local function SRWCCQZ_fake_script() -- AutoResetButton.LocalScript 
	local script = Instance.new('LocalScript', AutoResetButton)

	
	
	local button = script.Parent
	local Players = game:GetService("Players")
	local player = Players.LocalPlayer
	
	local autoResetEnabled = false
	local connection
	
	local function toggleAutoReset()
		autoResetEnabled = not autoResetEnabled
	
		if autoResetEnabled then
			button.Text = "AutoReset: ON"
	
			local function setupHumanoid(humanoid)
				connection = humanoid.HealthChanged:Connect(function(health)
					if autoResetEnabled and health < humanoid.MaxHealth then
	
						humanoid.Health = 0
					end
				end)
			end
	
	
			if player.Character then
				local humanoid = player.Character:FindFirstChildOfClass("Humanoid")
				if humanoid then
					setupHumanoid(humanoid)
				end
			end
	
	
			player.CharacterAdded:Connect(function(char)
				local humanoid = char:WaitForChild("Humanoid")
				setupHumanoid(humanoid)
			end)
		else
			button.Text = "AutoReset: OFF"
	
	
			if connection then
				connection:Disconnect()
				connection = nil
			end
		end
	end
	
	button.MouseButton1Click:Connect(toggleAutoReset)
	
	
	button.Text = "AutoReset: OFF"
	
end
coroutine.wrap(SRWCCQZ_fake_script)()
local function NFQIC_fake_script() -- SetSpawnButton.LocalScript 
	local script = Instance.new('LocalScript', SetSpawnButton)

	local button = script.Parent
	local Players = game:GetService("Players")
	local player = Players.LocalPlayer
	
	local stuckEnabled = false
	local stuckPosition = nil
	local loopConnection = nil
	
	local function startStuckLoop()
		if loopConnection then
			loopConnection:Disconnect()
		end
	
		loopConnection = game:GetService("RunService").Heartbeat:Connect(function()
			if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
				player.Character:MoveTo(stuckPosition)
			end
		end)
	end
	
	
	local function onCharacterAdded(character)
		if stuckEnabled and stuckPosition then
	
			character:WaitForChild("HumanoidRootPart")
			startStuckLoop()
		end
	end
	
	local function toggleStuck()
		stuckEnabled = not stuckEnabled
	
		if stuckEnabled then
			button.Text = "SetSpawn"
	
	
			local char = player.Character
			if char and char:FindFirstChild("HumanoidRootPart") then
				stuckPosition = char.HumanoidRootPart.Position
				startStuckLoop()
			end
	
			player.CharacterAdded:Connect(onCharacterAdded)
		else
			button.Text = "SetSpawn"
			stuckPosition = nil
			if loopConnection then
				loopConnection:Disconnect()
				loopConnection = nil
			end
		end
	end
	
	button.MouseButton1Click:Connect(toggleStuck)
	
end
coroutine.wrap(NFQIC_fake_script)()
local function AVHMN_fake_script() -- OptimizationButton.LocalScript 
	local script = Instance.new('LocalScript', OptimizationButton)

	local button = script.Parent
	local player = game:GetService("Players").LocalPlayer
	local StarterGui = game:GetService("StarterGui")
	
	local clicked = false
	
	button.MouseButton1Click:Connect(function()
		if clicked then return end
		clicked = true
	
		local map = workspace:FindFirstChild("Map")
		if map then
			local trees = map:FindFirstChild("Trees")
			local walls = map:FindFirstChild("Walls")
			local grasstop = map:FindFirstChild("GrassTop")
	
			if grasstop then
				grasstop:Destroy()
			end
			if trees then
				trees:Destroy()
			end
			if walls then
				walls:Destroy()
			end
		end
	
		
		button.Text = "Optimization"
		button.Active = false
		button.AutoButtonColor = false
	
		
		StarterGui:SetCore("SendNotification", {
			Title = "SYSTEM SAYS:",
			Text = "Succesfully made your game smoother !", -- You can change this text
			Duration = 4, -- how long the notification stays
		})
	end)
	
end
coroutine.wrap(AVHMN_fake_script)()
local function YTVCF_fake_script() -- RejoinButton.LocalScript 
	local script = Instance.new('LocalScript', RejoinButton)

	local button = script.Parent
	
	button.MouseButton1Click:Connect(function()
		local TeleportService = game:GetService("TeleportService")
		local Players = game:GetService("Players")
		local player = Players.LocalPlayer
		local placeId = game.PlaceId
	
		TeleportService:Teleport(placeId, player)
	end)
	
end
coroutine.wrap(YTVCF_fake_script)()
local function NMKVLDB_fake_script() -- KillsCountButton.LocalScript 
	local script = Instance.new('LocalScript', KillsCountButton)

	-- LocalScript inside your TextButton
	local button = script.Parent
	local frame = script.Parent.Parent:WaitForChild("KillsGainedFrame") -- change "Frame" if named differently
	
	-- optional: start hidden
	frame.Visible = false
	frame.BackgroundTransparency = 1
	
	-- tween service for smooth fade
	local TweenService = game:GetService("TweenService")
	
	-- settings
	local fadeTime = 0.3 -- seconds for fade animation
	
	-- function to fade in
	local function fadeIn()
		frame.Visible = true
		local tween = TweenService:Create(frame, TweenInfo.new(fadeTime), {BackgroundTransparency = 0})
		tween:Play()
	end
	
	-- function to fade out
	local function fadeOut()
		local tween = TweenService:Create(frame, TweenInfo.new(fadeTime), {BackgroundTransparency = 1})
		tween:Play()
		tween.Completed:Connect(function()
			frame.Visible = false
		end)
	end
	
	-- toggle on button click
	local isVisible = false
	button.MouseButton1Click:Connect(function()
		isVisible = not isVisible
		if isVisible then
			fadeIn()
		else
			fadeOut()
		end
	end)
	
end
coroutine.wrap(NMKVLDB_fake_script)()
local function HWHDVQO_fake_script() -- MainFrame.drag 
	local script = Instance.new('LocalScript', MainFrame)

	local UI = script.Parent
	
	local Dragging
	local Input
	local Start
	local StartPosition
	
	function Lerp(A, B, M)
		return A + (B - A) * M
	end
	
	local LMPos
	local LGPos
	
	local Speed = 13
	
	function Update(Time)
		if not StartPosition then
			return
		end
	
		if not Dragging and LGPos then
			UI.Position = UDim2.new(StartPosition.X.Scale, Lerp(UI.Position.X.Offset, LGPos.X.Offset, Time * Speed), StartPosition.Y.Scale, Lerp(UI.Position.Y.Offset, LGPos.Y.Offset, Time * Speed))
			return
		end
	
		local Delta = (LMPos - game:GetService("UserInputService"):GetMouseLocation())
	
		local xGoal = (StartPosition.X.Offset - Delta.X)
		local yGoal = (StartPosition.Y.Offset - Delta.Y)
	
		LGPos = UDim2.new(StartPosition.X.Scale, xGoal, StartPosition.Y.Scale, yGoal)
		UI.Position = UDim2.new(StartPosition.X.Scale, Lerp(UI.Position.X.Offset, xGoal, Time * Speed), StartPosition.Y.Scale, Lerp(UI.Position.Y.Offset, yGoal, Time * Speed))
	end
	
	UI.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			Dragging = true
			Start = input.Position
			StartPosition = UI.Position
			LMPos = game:GetService("UserInputService"):GetMouseLocation()
	
			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then
					Dragging = false
				end
			end)
		end
	end)
	
	UI.InputChanged:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
			Input = input
		end
	end)
	
	game:GetService("RunService").Heartbeat:Connect(Update)
end
coroutine.wrap(HWHDVQO_fake_script)()
local function NJGN_fake_script() -- KillsGained.LocalScript 
	local script = Instance.new('LocalScript', KillsGained)

	
	local label = script.Parent
	local Players = game:GetService("Players")
	local player = Players.LocalPlayer
	
	local kills = 0
	local gained = 0
	local resetTime = 4 -- seconds before gained resets
	
	-- Function to update the label text
	local function updateLabel()
		label.Text = string.format("Kills: %d (Gained: %d)", kills, gained)
	end
	
	updateLabel() -- initialize
	
	-- Function to simulate gaining a kill
	local function onKill()
		kills += 1
		gained += 1
		updateLabel()
	
		-- Wait 4 seconds then reset gained to 0
		task.delay(resetTime, function()
			gained = 0
			updateLabel()
		end)
	end
	
	-- 📊 Example: Connect this to your own kill detection
	-- Replace this with your actual kill event or system
	-- Example event you might use:
	-- player.leaderstats.Kills.Changed:Connect(onKill)
	-- or a remote event when player gets a kill
	
	-- TEMPORARY TEST: Press "K" to simulate a kill
	game:GetService("UserInputService").InputBegan:Connect(function(input, processed)
		if not processed and input.KeyCode == Enum.KeyCode.K then
			onKill()
		end
	end)
	
end
coroutine.wrap(NJGN_fake_script)()
local function SESEXJZ_fake_script() -- _0000.LocalScript 
	local script = Instance.new('LocalScript', _0000)

	
	local label = script.Parent
	local ui = label.Parent -- The parent frame/screen of the label; adjust if needed
	
	local runService = game:GetService("RunService")
	
	local timeElapsed = 0
	local counting = false
	
	-- Format function (MM:SS)
	local function formatTime(seconds)
		local minutes = math.floor(seconds / 60)
		local secs = seconds % 60
		return string.format("%02d:%02d", minutes, secs)
	end
	
	-- Update the text every frame
	runService.Heartbeat:Connect(function(dt)
		if counting then
			timeElapsed += dt
			label.Text = formatTime(math.floor(timeElapsed))
		end
	end)
	
	-- Detect when UI opens/closes
	ui:GetPropertyChangedSignal("Visible"):Connect(function()
		if ui.Visible then
			counting = true
		else
			counting = false
		end
	end)
	
	-- Start counting if UI starts visible
	if ui.Visible then
		counting = true
	end
	
end
coroutine.wrap(SESEXJZ_fake_script)()
local function XABKHR_fake_script() -- KillsGainedFrame.drag 
	local script = Instance.new('LocalScript', KillsGainedFrame)

	local UI = script.Parent
	
	local Dragging
	local Input
	local Start
	local StartPosition
	
	function Lerp(A, B, M)
		return A + (B - A) * M
	end
	
	local LMPos
	local LGPos
	
	local Speed = 10
	
	function Update(Time)
		if not StartPosition then
			return
		end
	
		if not Dragging and LGPos then
			UI.Position = UDim2.new(StartPosition.X.Scale, Lerp(UI.Position.X.Offset, LGPos.X.Offset, Time * Speed), StartPosition.Y.Scale, Lerp(UI.Position.Y.Offset, LGPos.Y.Offset, Time * Speed))
			return
		end
	
		local Delta = (LMPos - game:GetService("UserInputService"):GetMouseLocation())
	
		local xGoal = (StartPosition.X.Offset - Delta.X)
		local yGoal = (StartPosition.Y.Offset - Delta.Y)
	
		LGPos = UDim2.new(StartPosition.X.Scale, xGoal, StartPosition.Y.Scale, yGoal)
		UI.Position = UDim2.new(StartPosition.X.Scale, Lerp(UI.Position.X.Offset, xGoal, Time * Speed), StartPosition.Y.Scale, Lerp(UI.Position.Y.Offset, yGoal, Time * Speed))
	end
	
	UI.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			Dragging = true
			Start = input.Position
			StartPosition = UI.Position
			LMPos = game:GetService("UserInputService"):GetMouseLocation()
	
			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then
					Dragging = false
				end
			end)
		end
	end)
	
	UI.InputChanged:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
			Input = input
		end
	end)
	
	game:GetService("RunService").Heartbeat:Connect(Update)
end
coroutine.wrap(XABKHR_fake_script)()
local function UPNOYZ_fake_script() -- ImageButton.LocalScript 
	local script = Instance.new('LocalScript', ImageButton)

	
	local imageButton = script.Parent
	local screenGui = imageButton.Parent
	local mainFrame = screenGui:WaitForChild("MainFrame")
	
	imageButton.MouseButton1Click:Connect(function()
		mainFrame.Visible = true
		imageButton.Visible = false
	end)
	
end
coroutine.wrap(UPNOYZ_fake_script)()
local function DORHYI_fake_script() -- TextLabel_4.LocalScript 
	local script = Instance.new('LocalScript', TextLabel_4)

	-- LocalScript inside TextLabel
	local textLabel = script.Parent
	local Players = game:GetService("Players")
	local player = Players.LocalPlayer
	
	-- Set the TextLabel to show the player's name
	textLabel.Text = "Welcome, " .. player.Name .. "to Fz Hub!"
	
end
coroutine.wrap(DORHYI_fake_script)()
local function BCJRRMS_fake_script() -- FzHubV2.LocalScript 
	local script = Instance.new('LocalScript', FzHubV2)

	
	local gui = script.Parent
	local tweenService = game:GetService("TweenService")
	
	local welcomeFrame = gui:WaitForChild("WelcomeFrame")
	local mainFrame = gui:WaitForChild("MainFrame")
	
	-- Ensure initial visibility
	welcomeFrame.Visible = true
	mainFrame.Visible = false
	
	-- Wait 3 seconds before switching
	task.wait(3)
	
	-- Optional: Fade out welcome frame
	local fadeOut = tweenService:Create(
		welcomeFrame,
		TweenInfo.new(0.6, Enum.EasingStyle.Sine, Enum.EasingDirection.Out),
		{BackgroundTransparency = 1}
	)
	fadeOut:Play()
	
	-- Fade out text/image elements inside WelcomeFrame
	for _, obj in ipairs(welcomeFrame:GetDescendants()) do
		if obj:IsA("TextLabel") or obj:IsA("TextButton") then
			tweenService:Create(obj, TweenInfo.new(0.6), {TextTransparency = 1}):Play()
		elseif obj:IsA("ImageLabel") then
			tweenService:Create(obj, TweenInfo.new(0.6), {ImageTransparency = 1}):Play()
		end
	end
	
	fadeOut.Completed:Wait()
	welcomeFrame.Visible = false
	
	-- Show and fade in MainFrame
	mainFrame.Visible = true
	mainFrame.BackgroundTransparency = 1
	
	for _, obj in ipairs(mainFrame:GetDescendants()) do
		if obj:IsA("TextLabel") or obj:IsA("TextButton") then
			obj.TextTransparency = 1
		elseif obj:IsA("ImageLabel") then
			obj.ImageTransparency = 1
		end
	end
	
	local fadeIn = tweenService:Create(
		mainFrame,
		TweenInfo.new(0.6, Enum.EasingStyle.Sine, Enum.EasingDirection.Out),
		{BackgroundTransparency = 0}
	)
	fadeIn:Play()
	
	for _, obj in ipairs(mainFrame:GetDescendants()) do
		if obj:IsA("TextLabel") or obj:IsA("TextButton") then
			tweenService:Create(obj, TweenInfo.new(0.6), {TextTransparency = 0}):Play()
		elseif obj:IsA("ImageLabel") then
			tweenService:Create(obj, TweenInfo.new(0.6), {ImageTransparency = 0}):Play()
		end
	end
	
end
coroutine.wrap(BCJRRMS_fake_script)()
